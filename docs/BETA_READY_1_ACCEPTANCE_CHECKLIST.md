# BETA_READY_1_ACCEPTANCE_CHECKLIST

**文檔版本：** BETA-READY-1  
**建立日期：** 2026-05-27  
**適用專案：** Climbing Growth System  
**適用範圍：** v0.2-ECHO-PRESENCE-MVP 部署前驗收  

---

## 📋 驗收目的

**確保 v0.2-ECHO-PRESENCE-MVP 版本穩定可靠，準備進入 DEPLOY-1 階段。**

**驗收範圍：**
- ✅ 三大入口流程完整性
- ✅ Echo 系統新機制正確性
- ✅ 資料儲存和同步穩定性
- ✅ UI/UX 完整性
- ✅ 程式碼和部署安全性

---

## A. 必測清單 🔴

### **A1. 三入口流程 - CRITICAL**

#### **A1.1 Challenge Flow (💪)**
```
□ 點擊「💪 挑戰一個目標」→ 開啟 Challenge 表單
□ 輸入目標名稱 → 正常儲存
□ 選擇完成度（all/most/half/little）→ 正常運作
□ 輸入 Movement Notes → 正常儲存
□ 點擊「儲存挑戰」→ 成功提示 → 返回首頁
□ 首頁顯示新記錄卡片 → 格式正確
□ Challenge 記錄顯示目標和完成度 → 格式正確
□ Challenge 記錄下方有 Program 建立器 → 位置正確
```

#### **A1.2 Easy Flow (🌿)**
```
□ 點擊「🌿 慢慢來就好」→ 開啟 Easy 表單
□ 勾選 checkbox 選項（單選/多選）→ 正常運作
□ 輸入 Movement Notes → 正常儲存
□ 點擊「儲存記錄」→ 成功提示 → 返回首頁
□ 首頁顯示新記錄卡片 → 格式正確
□ Easy 記錄顯示勾選項目 → 格式正確
□ Easy 記錄沒有 Program 建立器 → 確認正確
```

#### **A1.3 Life Flow (☕)**
```
□ 點擊「☕ 好好過生活」→ 開啟 Life 表單
□ 選擇生活選項（radio button）→ 正常運作
□ 輸入生活描述 → 正常儲存
□ 點擊「儲存記錄」→ 成功提示 → 返回首頁
□ 首頁顯示新記錄卡片 → 格式正確
□ Life 記錄顯示選擇和描述 → 格式正確
□ Life 記錄沒有 Program 建立器 → 確認正確
```

### **A2. Echo 系統 - CRITICAL**

#### **A2.1 前3筆保證顯示**
```
□ 清空所有資料 → 確保乾淨狀態
□ 建立第1筆記錄（任一Flow）→ 必須有 Echo 顯示
□ 建立第2筆記錄（任一Flow）→ 必須有 Echo 顯示
□ 建立第3筆記錄（任一Flow）→ 必須有 Echo 顯示
□ 檢查 Echo 內容 → 符合風格憲法（無禁句）
```

#### **A2.2 第4筆後 Deterministic 70%**
```
□ 建立第4-10筆記錄（各種Flow）→ 約70%有Echo
□ 記錄哪些有/沒有 Echo → 驗證比例
□ 刷新頁面 → Echo 顯示狀態完全不變
□ 再次刷新 → Echo 顯示狀態完全不變
□ 檢查所有 Echo 文案 → 無禁句（明天、繼續、值得等）
```

#### **A2.3 文案品質檢查**
```
□ 檢查所有出現的 Echo 句子 → 無評價性語言
□ 檢查所有出現的 Echo 句子 → 無未來預測語言  
□ 檢查所有出現的 Echo 句子 → 無指導性語言
□ 檢查所有出現的 Echo 句子 → 符合溫柔見證風格
□ 不同 Flow 的 Echo → 路由邏輯正確
```

### **A3. 資料穩定性 - CRITICAL**

#### **A3.1 localStorage 功能**
```
□ 建立記錄 → localStorage 正確儲存
□ 刷新頁面 → 記錄完整載入
□ 編輯記錄 → localStorage 正確更新
□ 刪除記錄 → localStorage 正確移除
□ 檢查 localStorage 結構 → 無異常欄位
```

#### **A3.2 sessionStorage Guest 模式**
```
□ 點擊「訪客模式」→ 切換到 sessionStorage
□ 建立測試記錄 → sessionStorage 正確儲存
□ 刷新頁面 → 記錄正確載入
□ 關閉頁面重新開啟 → sessionStorage 資料清空（預期行為）
□ 從訪客模式切回 → localStorage 資料完整
```

#### **A3.3 Supabase Write（測試帳號）**
```
□ 登入測試帳號 → 正確連線 Supabase
□ 建立記錄 → Supabase 正確同步
□ 編輯記錄 → Supabase 正確更新
□ 刪除記錄 → Supabase 正確移除
□ 檢查 Supabase 表格 → 資料結構正確，無新欄位
```

#### **A3.4 debugDataQuality() 功能**
```
□ 開啟開發者工具 → console
□ 執行 app.debugDataQuality() → 無錯誤
□ 檢查輸出報告 → entries 數量正確
□ 檢查輸出報告 → 無資料結構異常
□ 檢查輸出報告 → Echo 相關欄位正常
```

### **A4. 資料管理功能 - CRITICAL**

#### **A4.1 CSV 匯出**
```
□ 點擊「匯出 CSV」→ 檔案正常下載
□ 開啟 CSV 檔案 → 資料完整，格式正確
□ 檢查 CSV 標頭 → 包含所有必要欄位
□ 檢查 CSV 內容 → Echo 相關資料正確匯出
```

#### **A4.2 JSON 匯出**
```
□ 點擊「匯出 JSON」→ 檔案正常下載
□ 檢查 JSON 格式 → 有效的 JSON 結構
□ 檢查 JSON 內容 → 所有 entries 完整匯出
□ 檢查 JSON 內容 → Echo 相關欄位正確
```

#### **A4.3 完整備份**
```
□ 點擊「完整備份」→ 檔案正常下載
□ 檢查備份檔案 → 包含完整應用程式狀態
□ 測試備份還原 → 資料完整恢復（手動驗證）
```

#### **A4.4 清除資料二次確認**
```
□ 點擊「清除所有資料」→ 出現二次確認對話框
□ 點擊「取消」→ 資料保持不變
□ 點擊「確認清除」→ 所有資料清空
□ 檢查 localStorage → 完全清空
□ 檢查頁面狀態 → 回到初始狀態
```

### **A5. UI 完整性 - CRITICAL**

#### **A5.1 首頁三入口**
```
□ 首頁顯示三個入口按鈕 → 💪 🌿 ☕
□ 按鈕文字正確 → 「挑戰一個目標」「慢慢來就好」「好好過生活」
□ 按鈕點擊正常 → 開啟對應表單
□ 按鈕樣式一致 → 無視覺異常
```

#### **A5.2 首頁定位文案**
```
□ 標題文案正確顯示 → 「攀岩生活記錄系統」或既定文案
□ 副標題或說明文案 → 正確顯示（如有）
□ 定位文案無錯字 → 檢查所有文字
□ 文案風格一致 → 符合產品定位
```

#### **A5.3 資料管理收合**
```
□ 「資料管理」區域預設收合 → 確認預設狀態
□ 點擊展開 → 正常展開功能選項
□ 點擊收合 → 正常收合隱藏選項
□ 收合狀態記憶 → 刷新後保持狀態（如有此功能）
```

#### **A5.4 Program 建立器位置**
```
□ Challenge 記錄卡片下方 → 有 Program 建立器
□ Easy 記錄卡片下方 → 無 Program 建立器
□ Life 記錄卡片下方 → 無 Program 建立器
□ Program 建立器功能 → 正常運作（如已實作）
```

### **A6. 安全確認 - CRITICAL**

#### **A6.1 程式碼安全**
```
□ 未碰 Pathly 相關程式碼 → 確認無 Pathly 修改
□ 未改 Supabase schema → 確認表格結構未變
□ 未改 cloud sync 邏輯 → 確認同步機制未變
□ 無新增外部依賴 → package.json 未變動
```

#### **A6.2 Git 狀態檢查**
```
□ git status → 無 untracked 異常檔案
□ git diff → 確認修改範圍僅限 app.js Echo 相關
□ 無意外修改其他檔案 → 只有 app.js 被修改
□ 確認提交訊息清晰 → 描述 Echo Presence MVP 功能
```

---

## B. 可選測試 🟡

### **B1. 進階 Echo 測試**
```
□ 測試大量記錄（100+）→ Echo 分布接近 70%
□ 測試極端時間戳 → 邊界情況正常處理
□ 測試空 entries 陣列 → 無 JavaScript 錯誤
□ 測試損壞的 entry 資料 → 有適當 fallback
```

### **B2. 跨瀏覽器測試**
```
□ Chrome → 所有功能正常
□ Firefox → 所有功能正常  
□ Safari → 所有功能正常（如可測試）
□ Edge → 所有功能正常
```

### **B3. 效能測試**
```
□ 大量資料載入速度 → 無明顯延遲
□ Echo 判定邏輯效能 → 無卡頓感
□ 記憶體使用檢查 → 無明顯泄漏
□ localStorage 大小檢查 → 合理範圍
```

### **B4. 用戶體驗測試**
```
□ 模擬新用戶流程 → 前3筆 Echo 體驗良好
□ 模擬老用戶流程 → Echo 變化不突兀
□ 模擬長期使用 → Echo 頻率感受合適
□ 檢查 Echo 文案多樣性 → 避免重複感
```

---

## C. 部署前阻擋項 🚫

### **C1. 功能性阻擋項**
- ❌ **任一入口流程無法正常建立記錄**
- ❌ **前3筆記錄未能保證顯示 Echo**
- ❌ **Echo 顯示狀態刷新後改變（非 deterministic）**
- ❌ **Echo 文案出現禁句（明天、繼續、值得等）**
- ❌ **資料無法正確儲存至 localStorage/Supabase**

### **C2. 安全性阻擋項**
- ❌ **意外修改了 Supabase schema**
- ❌ **意外修改了 cloud sync 邏輯**
- ❌ **git status 顯示意外的檔案修改**
- ❌ **出現 untracked 的異常檔案**

### **C3. 資料完整性阻擋項**
- ❌ **debugDataQuality() 報告資料結構異常**
- ❌ **CSV/JSON 匯出功能失效**
- ❌ **清除資料功能失效或缺少確認**
- ❌ **既有記錄在新版本下顯示異常**

### **C4. UI/UX 阻擋項**
- ❌ **Program 建立器出現在錯誤位置**
- ❌ **三入口按鈕無法正常點擊**
- ❌ **首頁出現明顯的 UI 錯位或錯誤**
- ❌ **Console 出現 JavaScript 錯誤**

---

## D. 部署決策矩陣

### **DEPLOY-1 進入條件**

#### **✅ 可以進入 DEPLOY-1**
```
條件：
✅ A. 必測清單 → 100% 通過
✅ C. 部署前阻擋項 → 0 個觸發
✅ 核心功能穩定 → 三入口 + Echo + 資料管理
✅ 安全檢查通過 → 無意外修改
```

#### **⏸️ 暫緩 DEPLOY-1**
```
條件：
❌ A. 必測清單 → <95% 通過
❌ C. 部署前阻擋項 → ≥1 個觸發
❌ 發現新的 critical bug
❌ Echo 邏輯存在重大問題
```

#### **🚫 禁止 DEPLOY-1**
```
條件：
❌ 資料完整性問題 → 可能導致資料遺失
❌ 安全性問題 → 可能影響既有功能
❌ Echo 系統完全失效
❌ 三入口流程任一失效
```

### **DEPLOY-1 部署建議**

#### **如果全部通過：**
```
1. 【立即可行】→ 進入 DEPLOY-1
2. 【部署範圍】→ 測試環境小範圍部署
3. 【監控重點】→ Echo 顯示率、用戶反應、系統穩定性
4. 【回滾準備】→ 保留上一版本，準備快速回滾方案
5. 【下一步】→ 收集測試用戶 Echo 體驗回饋
```

#### **如果部分問題：**
```
1. 【優先修復】→ C. 阻擋項（如有）
2. 【次要修復】→ A. 必測清單未通過項目
3. 【延後處理】→ B. 可選測試問題
4. 【重新驗收】→ 修復後重跑必測清單
5. 【再次評估】→ 確認可進入 DEPLOY-1
```

---

## 📊 驗收記錄表

### **執行資訊**
- **執行日期：** ___________
- **執行人員：** ___________
- **測試環境：** ___________
- **Git Commit：** ___________

### **結果統計**
- **A. 必測清單通過率：** _____ / _____ （_____%)
- **B. 可選測試通過率：** _____ / _____ （_____%)
- **C. 阻擋項觸發數量：** _____ 個
- **發現問題數量：** _____ 個

### **最終決策**
- **□ 通過，可進入 DEPLOY-1**
- **□ 部分通過，修復後重測**
- **□ 未通過，需要重大修改**

### **問題記錄**
```
問題1：
□ 問題描述：
□ 嚴重等級：Critical / Major / Minor
□ 修復狀態：待修復 / 已修復 / 已驗證

問題2：
□ 問題描述：
□ 嚴重等級：Critical / Major / Minor
□ 修復狀態：待修復 / 已修復 / 已驗證
```

### **下一步行動**
```
□ 立即部署到測試環境
□ 修復問題後重新驗收
□ 通知相關人員驗收結果
□ 準備 DEPLOY-1 部署計劃
```

---

*最後更新：2026-05-27*  
*文檔狀態：✅ 驗收清單就緒*  
*使用狀態：⏳ 待執行驗收*  
*下一步：執行必測清單*